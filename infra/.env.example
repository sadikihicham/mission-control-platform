# Copier en infra/.env (gitignoré) et mettre de vraies valeurs.
# docker compose charge automatiquement ce fichier (même dossier que le compose).
JWT_SECRET=change-me-with-a-long-random-string
MC_INGEST_TOKEN=change-me-ingest-token
